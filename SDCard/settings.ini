// Gargoyle settings. All timings are in seconds.
{
	// Idle mode settings
	Idle: { 
		// Color to lit when Idle
		Color: [0, 255, 0], 
		// Say random sound from IdleSnd folder at random interavls. 
		SoundPeriod: {Min: 4, Max: 12} 
	},

	// Signal settings
	Signal: {
		// Eye flash settings
		Color: [255, 0, 0], 
		Duration: 4	
	}
}